using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DocumentSystem
{
    public class Command
    {
    //    public readonly char[] AttributeSeparators = new char[] { '[', ']', ';'};

    //    string Name { get; private set; }
    //    public List<KeyValuePair<string, string>> AttributeSeparators
    //    {
    //        get
    //        {
    //            List<KeyValuePair<string, string>> attributesCopy = new List<KeyValuePair<string,string>>();

    //            foreach (var item in AttributeSeparators)
    //{
    //                attributesCopy.Add(new KeyValuePair<string,string>(item.Key, item.Value));
    //}
    //            return
    //        }
    //    }

    //    public Command(string name, List<KeyValuePair<string,string>> attributes)
    //    {
    //        this.Name = name;
    //        this.Attributes = attributes;
    //    }

    //    public static Command Parse(string commandString)
    //    {
    //        string[] commandAndAttributesSplit = commandString.Split(AttributeSeparators);
    //        string commandName = commandAndAttributesSplit[0];

    //        List<KeyValuePair<string, string>> attributes = new List<KeyValuePair<string,string>>();

    //        for (int i = 0; i < commandAndAttributesSplit.Length; i++)
    //        {
    //            string[] keyValuePairSplit = commandAndAttributesSplit[i].Split(KeyValuePair);
    //            if (true)
    //            {
                    
    //            }
    //        }
    //    }
    }
}
